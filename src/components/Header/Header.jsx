import { Navbar } from "../../index";
import "./Header.scss";

const Header = () => {
    return (
        <header>
            <h1>SALVADOR DALI</h1>
            <Navbar />
        </header>
    );
};

export default Header;
